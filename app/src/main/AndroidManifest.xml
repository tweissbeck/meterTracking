<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="meter.tracking">

    <!-- Used to reset alarm when device restart (aka notification) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:name=".MetricApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".launch.LaunchActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".metrics.main.MetersTrackingActivity" />
        <activity
            android:name=".metrics.create.CreateNewMetricActivity"
            android:parentActivityName=".metrics.main.MetersTrackingActivity" />
        <activity
            android:name=".metrics.detail.MetricDetailActivity"
            android:parentActivityName=".metrics.main.MetersTrackingActivity" />
        <activity
            android:name=".notification.config.NotificationConfigActivity"
            android:parentActivityName=".metrics.main.MetersTrackingActivity" />
        <receiver android:name="meter.tracking.notification.NotificationsBootReceiver"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
        </receiver>
    </application>

</manifest>